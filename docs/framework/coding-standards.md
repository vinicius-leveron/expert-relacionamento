# Coding Standards — Perpétuo

> **Status**: pending — populated after Phase 3 (`@architect`).
>
> Esta página será preenchida quando a stack estiver decidida (Node.js + framework + ORM + lint/format) e quando o tech preset `nodejs-backend.md` for derivado de `_template.md`.
>
> Até lá, NÃO escreva código de produção neste repo.
